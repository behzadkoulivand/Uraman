<% if (stays.length > 0) { %>
<h2 class="mt-2">آخرین پست ها</h2>
<div class="table-responsive">
    <table class="table table-striped table-sm">
        <thead>
            <tr>
                <th>#</th>
                <th>عنوان</th>
                <th>تاریخ ورود</th>
                <th>تاریخ خروج</th>
            </tr>
        </thead>
        <tbody>
            <% for (let stay of stays) { %>
            <tr>
                <td>1</td>
                <td><a href="/stays/<%= stay._id %>"><%= stay.title %></a></td>
                <td><%= formatDate(stay.entryDate) %></td>
                <td><%= formatDate(stay.exitDate) %>  </td>
                <td>
                    <a href="/dashboard/edit-stay/<%= stay._id %>" class="btn btn-sm btn-primary">ویرایش</a>
                </td>
                <td>
                    <a href="/dashboard/delete-stay/<%= stay._id %>" class="btn btn-sm btn-danger">حذف</a>
                </td>
            </tr>
            <% } %>
        </tbody>
    </table>
</div>
<% } else { %>
<div class="text-center">
    <h3 class="mt-2">
        در حال حاضر هیچ اقامتی توسط شما در پایگاه داده ثبت نشده است
    </h3>
</div>
<% } %>
